[project]
name = "Crazyflie-Flower-Swarm"
version = "0.1.0"
description = "Example of running a Crazyflie swarm using the Crazyflie Rust library"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "matplotlib",
    "PyQt6",
]
